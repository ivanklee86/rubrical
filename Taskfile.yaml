# https://taskfile.dev
version: '3'

vars:
  GREETING: Hello, World!

includes:
  python: ./tasks/python.yaml
  docker: ./tasks/docker.yaml
  integration: ./tasks/interation.yaml

tasks:
  default:
    cmds:
      - task: python:install
      - task: python:lint
      - task: python:test
